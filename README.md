# automate
PRETSA ansible automations

## Development

Build
```bash
ansible-galaxy collection build --force pretsa/automate
```

Install
```bash
ansible-galaxy collection install --force /path/to/pretsa-automate-x.y.z.tar.gz
```
